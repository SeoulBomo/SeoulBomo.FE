export const metadata = {
  title: "서울보모 : 로그인",
  description: "서울보모 로그인 페이지",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
